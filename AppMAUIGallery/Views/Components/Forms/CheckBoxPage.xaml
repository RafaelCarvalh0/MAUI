<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppMAUIGallery.Views.Components.Forms.CheckBoxPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="CheckBoxPage">
    <VerticalStackLayout>
        <Label
            HorizontalOptions="Center"
            Text="Qual a linguagem que você mais gosta?"
            VerticalOptions="Center" />
        <HorizontalStackLayout>
            <CheckBox CheckedChanged="CheckBox_CheckedChanged" Color="MediumPurple" />
            <Label Text="PHP" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox CheckedChanged="CheckBox_CheckedChanged" Color="Purple" />
            <Label Text="C#" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox CheckedChanged="CheckBox_CheckedChanged" Color="Orange" />
            <Label Text="JAVA" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox CheckedChanged="CheckBox_CheckedChanged" Color="OrangeRed" />
            <Label Text="RUSTY" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <Label x:Name="LblStatus" />
    </VerticalStackLayout>
</ContentPage>